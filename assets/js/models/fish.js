class Fish {

    constructor(ctx, x, y) {
        /*Atributos de posición y tamaño en el canvas*/
        /*Atributos de nivel en el juego*/
        /*Atributos de aporte de alimento (INPUT)*/
        /*Atributos sprite*/
        /*Atributos de movimiento*/
        /*Atributos de estado : vivo/comiendo/muerto*/
    }
    isReady() {
        return this.sprite.isReady;
      } 
      clear() {
      }
      draw() {
      }
      move() {
      }
      animate() {
      }
}